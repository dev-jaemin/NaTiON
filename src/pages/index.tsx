import type { NextPage } from "next";
import Main from "../component/layout/Main";

const Home: NextPage = () => {
    return (
        <div>
            <Main />
        </div>
    );
};

export default Home;
